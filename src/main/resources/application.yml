spring:
  application:
    name: merca-recife
  servlet:
    multipart:
      max-file-size: 20MB

  datasource:
    url: jdbc:postgresql://localhost:5432/merca-recife
    username: merca
    password: recife123
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true
    locations: classpath:db/migration


file:
  upload-dir: src/main/resources/uploads
